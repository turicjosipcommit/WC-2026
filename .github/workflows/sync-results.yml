name: Sync WC results

on:
  schedule:
    # Every 5 minutes — gated to active match windows (+ fallback at :00/:30 UTC)
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
      SYNC_FORCE: ${{ github.event_name == 'workflow_dispatch' && '1' || '' }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Sync results when matches are live or due
        run: npm run sync:results:if-needed

      - name: Ping deployed app sync endpoint (optional)
        if: vars.APP_URL != ''
        env:
          APP_URL: ${{ vars.APP_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          curl -fsS -X POST "$APP_URL/api/internal/sync-results" \
            -H "x-cron-secret: $CRON_SECRET" || true
